# Contributing

Never include a real credential, secret key, `.env`, database dump, or production metadata in issues, commits, fixtures, or screenshots. Run `make verify` before opening a pull request. Security-sensitive changes require tests and a threat-model update where applicable. Report vulnerabilities privately as described in [SECURITY.md](SECURITY.md).

